# harness/adapters/tool_registry/__init__.py
#
# Package marker. The ToolRegistry Protocol lives in
# harness/tools/registry.py (canonical location). The reference
# in-memory implementation lives in this directory.
