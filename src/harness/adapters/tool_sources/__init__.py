# harness/adapters/tool_sources/__init__.py
#
# Package marker. The ToolSource Protocol lives in base.py. Reference
# implementations (local, skill) are sibling files. The SourceRegistry
# — which manages activation and the ScopedRegistryView lifecycle —
# also lives here as it is the implementation detail of load_sources.
