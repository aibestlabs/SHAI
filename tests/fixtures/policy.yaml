# tests/fixtures/policy.yaml — rule set the tests run against.
#
# RESPONSIBILITY
#   A small but representative rule set that exercises every branch of
#   the reference RuleBasedPolicy: tool_tags matching, tool_names
#   matching, tenants filtering, allow/deny/redact actions, and
#   default-allow fallthrough.
#
# WHAT TO IMPLEMENT
#   - A deny rule on tools tagged "external_write" for tenant "t1"
#     (used by deny-path tests).
#   - A redact rule that scrubs a known field in a tool's args (used
#     by redact-path tests).
#   - An allow rule explicit enough that allow-path tests don't have
#     to rely on default fallthrough.
#   - At least one rule that uses an `all` boolean combinator.
#
# DO NOT
#   - Add rules that aren't exercised by a test. The fixture exists for
#     tests; speculative rules drift out of sync.
