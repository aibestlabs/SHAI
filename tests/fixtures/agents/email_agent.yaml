# tests/fixtures/agents/email_agent.yaml
# Canonical test agent fixture — email agent with external_write capability.
# Used by test_agent_registry.py, test_agent_config.py, and integration tests.
#
# WHAT TO IMPLEMENT
#   Fill in a minimal but complete valid AgentConfig:
#   - id: email_agent
#   - allowed_tags covering read + external_write
#   - allowed_tool_names listing send_email and search_docs
#   - sources referencing at least one source name from fixtures/harness.yaml
#   - One allow rule and one deny rule in policy_rules
#   - log_level: DEBUG (to distinguish from default in tests)
#   - audit_tags: {team: "test"}
